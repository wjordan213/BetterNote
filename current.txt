Search:
  Todo(in order):
    -create backbone view on sidepane for note search
    -remove search bar from side pane for notes index
    -have note search button link to note search sidepane, implement in router because it will be less painful
    -have search send out collection fetch or something like that
    -position search button correctly

  implementation specifics:
    Backbone:
      Views:
        -html: input and button
          -remove search bar from notes indexes
          -side pane view for that
        -listeners: click button
        -colllection listener 'sync' : renderResults()
          -


      Collections:
        -SearchResults collection
          -model: Note
          -url: '/api/search'
        -searchInfo.query = $input.val();
        -fetched on search event
          -data: this.collection.searchInfo
        -Sync event triggers View method
